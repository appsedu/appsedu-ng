#!/bin/bash

pkexec /usr/share/appsedu/parley.sh 2>&1 >/dev/null | \
zenity --progress --pulsate --no-cancel --auto-close --title="Parley" --width=450 \
--text="Espere um pouco. Estamos instalando o app..."

parley
