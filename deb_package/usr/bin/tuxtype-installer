#!/bin/bash

pkexec /usr/share/appsedu/tuxtype.sh 2>&1 >/dev/null | \
zenity --progress --pulsate --no-cancel --auto-close --title="TuxType" --width=450 \
--text="Espere um pouco. Estamos instalando o app..."

tuxtype
