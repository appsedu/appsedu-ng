#!/bin/bash

pkexec /usr/share/appsedu/gresistor.sh 2>&1 >/dev/null | \
zenity --progress --pulsate --no-cancel --auto-close --title="gResistor" --width=450 \
--text="Espere um pouco. Estamos instalando o app..."

gresistor
