#!/bin/bash

pkexec /usr/share/appsedu/planets.sh 2>&1 >/dev/null | \
zenity --progress --pulsate --no-cancel --auto-close --title="Planets" --width=450 \
--text="Espere um pouco. Estamos instalando o app..."

planets
