#!/bin/bash

pkexec /usr/share/appsedu/openuniverse.sh 2>&1 >/dev/null | \
zenity --progress --pulsate --no-cancel --auto-close --title="OpenUniverse" --width=450 \
--text="Espere um pouco. Estamos instalando o app..."

openuniverse
