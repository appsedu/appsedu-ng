#!/bin/bash

pkexec /usr/share/appsedu/stellarium.sh 2>&1 >/dev/null | \
zenity --progress --pulsate --no-cancel --auto-close --title="Stellarium" --width=450 \
--text="Espere um pouco. Estamos instalando o app..."

stellarium
