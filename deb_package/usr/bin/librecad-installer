#!/bin/bash

pkexec /usr/share/appsedu/librecad.sh 2>&1 >/dev/null | \
zenity --progress --pulsate --no-cancel --auto-close --title="LibreCAD" --width=450 \
--text="Espere um pouco. Estamos instalando o app..."

librecad
