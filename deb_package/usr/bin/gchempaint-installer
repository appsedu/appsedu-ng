#!/bin/bash

pkexec /usr/share/appsedu/gchempaint.sh 2>&1 >/dev/null | \
zenity --progress --pulsate --no-cancel --auto-close --title="GChemPaint" --width=450 \
--text="Espere um pouco. Estamos instalando o app..."

gchempaint
