#!/bin/bash

pkexec /usr/share/appsedu/chemtool.sh 2>&1 >/dev/null | \
zenity --progress --pulsate --no-cancel --auto-close --title="ChemTool" --width=450 \
--text="Espere um pouco. Estamos instalando o app..."

chemtool
