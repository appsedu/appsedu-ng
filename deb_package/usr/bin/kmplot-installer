#!/bin/bash

pkexec /usr/share/appsedu/kmplot.sh 2>&1 >/dev/null | \
zenity --progress --pulsate --no-cancel --auto-close --title="KmPlot" --width=450 \
--text="Espere um pouco. Estamos instalando o app..."

kmplot
