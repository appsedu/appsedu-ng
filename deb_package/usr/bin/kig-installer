#!/bin/bash

pkexec /usr/share/appsedu/kig.sh 2>&1 >/dev/null | \
zenity --progress --pulsate --no-cancel --auto-close --title="Kig" --width=450 \
--text="Espere um pouco. Estamos instalando o app..."

kig
