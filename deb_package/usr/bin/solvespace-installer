#!/bin/bash

pkexec /usr/share/appsedu/solvespace.sh 2>&1 >/dev/null | \
zenity --progress --pulsate --no-cancel --auto-close --title="SolveSpace" --width=450 \
--text="Espere um pouco. Estamos instalando o app..."

solvespace
