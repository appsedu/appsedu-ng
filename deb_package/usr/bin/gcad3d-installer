#!/bin/bash

pkexec /usr/share/appsedu/gcad3d.sh 2>&1 >/dev/null | \
zenity --progress --pulsate --no-cancel --auto-close --title="gCAD3D" --width=450 \
--text="Espere um pouco. Estamos instalando o app..."

gcad3d
