#!/bin/bash

pkexec /usr/share/appsedu/irydium-chemistry-lab.sh 2>&1 >/dev/null | \
zenity --progress --pulsate --no-cancel --auto-close --title="Irydium Chemistry Lab" --width=450 \
--text="Espere um pouco. Estamos instalando o app..."

irydium-chemistry-lab
