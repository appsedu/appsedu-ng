#!/bin/bash

pkexec /usr/share/appsedu/tuxmath.sh 2>&1 >/dev/null | \
zenity --progress --pulsate --no-cancel --auto-close --title="TuxMath" --width=450 \
--text="Espere um pouco. Estamos instalando o app..."

tuxmath
