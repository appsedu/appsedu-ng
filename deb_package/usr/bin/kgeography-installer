#!/bin/bash

pkexec /usr/share/appsedu/kgeography.sh 2>&1 >/dev/null | \
zenity --progress --pulsate --no-cancel --auto-close --title="KGeography" --width=450 \
--text="Espere um pouco. Estamos instalando o app..."

kgeography
