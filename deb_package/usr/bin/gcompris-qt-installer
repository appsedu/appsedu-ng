#!/bin/bash

pkexec /usr/share/appsedu/gcompris-qt.sh 2>&1 >/dev/null | \
zenity --progress --pulsate --no-cancel --auto-close --title="GCompris" --width=450 \
--text="Espere um pouco. Estamos instalando o app..."

gcompris-qt
