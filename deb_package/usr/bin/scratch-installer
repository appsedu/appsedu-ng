#!/bin/bash

pkexec /usr/share/appsedu/scratch.sh 2>&1 >/dev/null | \
zenity --progress --pulsate --no-cancel --auto-close --title="Scratch" --width=450 \
--text="Espere um pouco. Estamos instalando o app..."

scratch
