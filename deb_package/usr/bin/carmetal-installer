#!/bin/bash

pkexec /usr/share/appsedu/carmetal.sh 2>&1 >/dev/null | \
zenity --progress --pulsate --no-cancel --auto-close --title="CaRMetal" --width=450 \
--text="Espere um pouco. Estamos instalando o app..."

carmetal
