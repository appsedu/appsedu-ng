#!/bin/bash

pkexec /usr/share/appsedu/gconjugue.sh 2>&1 >/dev/null | \
zenity --progress --pulsate --no-cancel --auto-close --title="GConjugue" --width=450 \
--text="Espere um pouco. Estamos instalando o app..."

gconjugue
